/**
 * Clase base para todos los Componentes.
 * Sirve principalmente para identificación de tipo.
 */
export default class Component {
    constructor() {
        // La clase base puede estar vacía.
    }
} 